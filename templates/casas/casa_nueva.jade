{% extends "base.jade" %}

{% block page_title %}
| Añadir casa
{% endblock %}

{% block section_title %}
{% if title %}
{{ title }}
{%else%}
| Añadir casa
{% endif%}
{% endblock %}

{% block content %}

{% if messages %}
{% for message in messages %}
{% if message.tags == "error" %}
div(class="alert-{{message.tags}} container") {{message}}
  i.fa.fa-minus-circle
{% else %}
{{messages.tags}}
div(class="alert-{{message.tags}} container") {{message}}
  i.fa.fa-check-circle-o
{% endif %}
{% endfor %}
{% endif %}
form(action="", method="POST", enctype="multipart/form-data", class="container")
  | {% csrf_token %}
  div.row
    div.twelve.columns
      label(for="{{form.nombre.id_for_label}}") Nombre
      {{form.nombre}}

  div.row
    div.four.columns
      label(for="{{form.direccion.id_for_label}}") Direccion
      {{form.direccion}}
    div.four.columns
      label(for="{{form.ciudad.id_for_label}}") Ciudad
      div.relative-box
        {{form.ciudad}}
    div.four.columns
      label(for="{{form.postcode.id_for_label}}") CP
      {{form.postcode}}
  div.row  
    div.four.columns
      label(for="{{form.numeroHabitaciones.id_for_label}}") Habitaciones
      {{form.numeroHabitaciones}}
    div.four.columns
      label(for="{{form.numeroBanios.id_for_label}}") Baños
      {{form.numeroBanios}}
    div.four.columns
      {{form.imagen}}
      label(for="{{form.imagen.id_for_label}}") 
        i.fa.fa-upload   Subir Imagen
  div.check-icons
    div.roundedcheck
      {{form.publico}}
      label(for="{{form.publico.id_for_label}}" class="eye")
    div.roundedcheck
      {{form.wifi}}
      label(for="{{form.wifi.id_for_label}}", class="wifi")
    div.roundedcheck
      {{form.parking}}
      label(for="{{form.parking.id_for_label}}" class="parking")
    div.roundedcheck
      {{form.piscina}}
      label(for="{{form.piscina.id_for_label}}" class="pool")
    div.roundedcheck
      {{form.mascotas}}
      label(for="{{form.mascotas.id_for_label}}" class="pet")
  div.row
    div.twelve.columns
      label(for="{{form.descripcion.id_for_label}}") Descripcion
      {{form.descripcion}}
  div.row
    button(type="submit" class="button-primary twelve columns") Confirmar registro
div.results
{% endblock %}
